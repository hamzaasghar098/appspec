version: 0.0

phases:
  install:
    runtime-versions:
      python: 3.8  # Example runtime version specification
    commands:
      - echo "Installing dependencies, if any"
      # Add any commands here needed to install dependencies for your application

  pre_build:
    commands:
      - echo "Executing pre-build commands"
      # Add any pre-build commands here

  build:
    commands:
      - echo "Build started"
      # Add build commands here if applicable

  post_build:
    commands:
      - echo "Build completed"
      - echo "Copying files to /var/www/html"
      - cp -r * /var/www/html
      # Add any post-build commands here

artifacts:
  files:
    - '**/*'
  base-directory: /
